<?xml version="1.0"?>
<data>
    <query id="get_count">
		SELECT count(*)
		FROM baseball.message_history
		WHERE game_id = '{game_id}'
		AND group_id = '{group_id}'
		AND paratemters = '{paratemters}'
		LIMIT 1
    </query>
	
	<query id="insert_msg">
		INSERT INTO baseball.message_history (game_id, group_id, message, parameters, date)
		VALUES('{0}', '{1}', '{2}', '{3}', DATE_FORMAT(NOW(), "%Y%m%d%H%i%S"))
    </query>
	
	<query id="update_msg_count">
		UPDATE baseball.message_history 
		SET use_count = use_count + 1
		WHERE group_id = '{0}'
		AND subject = '{1}'
		AND msg_param = '{2}'
    </query>

</data>